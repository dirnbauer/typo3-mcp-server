# Archived one-off scripts

These scripts performed the XLIFF 1.2 → 2.0 ICU migration for `locallang_mod.xlf`
(May 2026). The migration is complete; keep them here for reference only.

- `convert-locallang-mod-to-xliff2.php` — converted English and German module labels
- `de-locallang-mod-overrides.php` — German ICU overrides applied during conversion

Do not run against current tree unless you intentionally revert the XLF files first.
